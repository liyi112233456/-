# 3424 根钢筋箱梁样例结果

该目录是端到端验证生成的结果，不包含用户原始 IFC 文件。

- `output/viewer_model.json`：浏览器三维动画数据；
- `output/installation_sequence.csv`：安装顺序；
- `output/robot/tcp_trajectory.csv`：完整 TCP 轨迹；
- `result_bundle.zip`：后台任务生成的下载包。
