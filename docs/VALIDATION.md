# 端到端验证记录

验证日期：2026-08-05

## 测试模型

`预制有砟轨道后张法预应力混凝土简支箱梁（1(2).ifc`

- 文件大小：约 20 MB；
- IFC Schema：IFC2X3；
- STEP 实体：391,535；
- 钢筋实例：3,424；
- 可复用轴线几何：169；
- 轴线总长度：9,831.489 m；
- 钢筋直径：8–20 mm。

## 端到端耗时

以下 26.3 s 是 v1.0 单调高度拓扑流程的历史基线，不含新增 SE(3) 碰撞检查；新流程耗时取决于碰撞离散步长、弯曲钢筋段数、人工顺序可行性和 RRT 触发次数。

实际耗时受 CPU、磁盘和容器资源限制影响。

## 机器人输出

- TCP 采样点：96,293；
- 名义运动时长：8,873.058 s；
- ABB RAPID、KUKA KRL、URScript 均已生成；
- 当前为任务空间轨迹，未绑定具体机器人 URDF 和关节约束。

## 自动测试

- IFC 几何辅助函数与二维线段最近点；
- 三维线段胶囊体距离；
- 刚体四元数姿态变换；
- Excel 人工顺序重排、方向读取和完整性校验；
- X/Y/Z 多方向自动拓扑；
- 小型模型的 SE(3) 路径与碰撞报告输出；
- `/api/health` 与前端主页静态服务；
- 前端 JavaScript 语法检查。

结果：10 项 Python 测试全部通过，前端语法检查通过。

## 工程边界

`strict_graph_feasible=true` 仅说明多方向投影扫掠阻挡图不需要强制拆环；`assembly_collision.all_paths_collision_free=true` 说明钢筋中心轴胶囊体在给定平移/旋转离散步长下通过。两者都不代表模板、混凝土、夹具和机器人完整连杆模型下已经取得现场安全证书。
